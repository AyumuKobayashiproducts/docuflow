# DocuFlow - .env.local template
#
# Usage:
#   cp docs/env.example .env.local
#   # then edit .env.local
#
# Notes:
# - NEXT_PUBLIC_* variables are exposed to the browser.
# - Do NOT commit .env.local (this repo ignores .env* by default).
#

# -------------------------
# Supabase (Required)
# -------------------------
# Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional (server-side only): enables admin operations (account deletion, share audit logs, etc.)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# -------------------------
# App Auth Cookie (Required)
# -------------------------
# Server-issued, signed session cookie secret (HMAC-SHA256).
# - Required in production (Vercel).
# - Use a long random string (32+ chars). Example generation (macOS):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_COOKIE_SECRET=REPLACE_WITH_RANDOM_SECRET

# -------------------------
# Site URL (Recommended)
# -------------------------
# Used for auth/email links & billing redirect URLs.
# Local example:
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Production example:
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app

# -------------------------
# OpenAI (Optional)
# -------------------------
# If missing, AI features are disabled.
OPENAI_API_KEY=sk-REPLACE_ME

# -------------------------
# Stripe Billing (Optional)
# -------------------------
# If missing, billing UI and subscription flows will be limited/disabled.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_or_test_xxx
STRIPE_SECRET_KEY=sk_live_or_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Price IDs (Stripe Dashboard → Products → Pricing)
STRIPE_PRICE_PRO_MONTH=price_xxx
STRIPE_PRICE_TEAM_MONTH=price_xxx
STRIPE_PRICE_ENTERPRISE_MONTH=price_xxx

# -------------------------
# Admin / Demo (Optional)
# -------------------------
# Lock admin pages to a specific Supabase Auth user_id (UUID).
DOCUFLOW_OWNER_USER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Used by demo seed scripts (optional).
DEMO_USER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx



